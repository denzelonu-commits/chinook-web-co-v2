## Project Setup
This is a Next.js + TypeScript project using Geist font. When creating components, always use TypeScript (.tsx), ensure builds pass with `npm run build`, and check for CSS/TS errors before considering work done.
